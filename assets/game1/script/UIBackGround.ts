import { _decorator, Component, Node } from 'cc';
import {UIView} from "db://assets/libs/ui/UIView";
const { ccclass, property } = _decorator;

@ccclass('UIBackGround')
export class UIBackGround extends UIView {
    start() {

    }

    update(deltaTime: number) {
        
    }
}

